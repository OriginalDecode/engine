#pragma once
#include "snowblind_shared.h"
class Level
{
public:
	Level() = default;

	void Update(float dt);

	/* 
		What does a level contain?

		It's basically a scene,
		there is a bunch of entities that are connected to it.
		More or less it's just a file which loads specific entities to said positions.
		Might involve scripts and such as well?
	
	*/


};

